if (typeof equip_days_until_check === "undefined" || equip_days_until_check === null || equip_days_until_check === "") { return; }  

if (equip_days_until_check > 1){
    var formattedText = "Success"
}
else {
    var formattedText = "Error"; 
}

return formattedText;