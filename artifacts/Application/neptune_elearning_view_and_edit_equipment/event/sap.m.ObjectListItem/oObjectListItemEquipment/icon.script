//if (typeof assigned_for_checking === "undefined" || assigned_for_checking === null || assigned_for_checking === "") { return; }  

if (assigned_for_checking === true){
    var formattedText = "sap-icon://activity-individual"; 
} else {
    var formattedText = "sap-icon://activity-items"; 
}


return formattedText;